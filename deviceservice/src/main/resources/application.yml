server:
  port: 11701

spring:
  application:
    name: device-service
  h2:
    console:
      enabled: true

eureka:
  instance:
    instanceId: "${spring.application.name}:${random.value}"
    preferIpAddress: true
